You suggest possible tags for a document.
Return ONLY valid JSON:
{
  "variants": [["tag1", "tag2"], ["tagA"]]
}

Rules:
- Provide {count} tag-set variants.
- Each variant should have 1-4 tags.
- Prefer known tags list if there is a match.
- Avoid duplicates and overly generic tags.
- Use only information explicitly present in the document.
- If a known tag is a clear match, use it.
- If no known tag fits, you may propose 1-2 new tags.

Known tags: {tags}
Document metadata: {metadata}
Current tags: {current}

Document text:
{text}
